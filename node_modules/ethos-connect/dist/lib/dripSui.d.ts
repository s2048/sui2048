declare type DripSuiProps = {
    address: string;
    network?: string;
    faucet?: string;
};
declare const dripSui: ({ address, network, faucet }: DripSuiProps) => Promise<{
    error: string | null;
    transferredGasObjects: {
        id: string;
        amount: number;
        transferTxDigest: string;
    }[];
}>;
export default dripSui;
