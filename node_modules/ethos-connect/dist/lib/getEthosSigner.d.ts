import { HostedSigner } from '../types/Signer';
import type { JsonRpcProvider } from '@mysten/sui.js';
import { Chain } from 'enums/Chain';
declare const getEthosSigner: ({ provider, defaultChain }: {
    provider: JsonRpcProvider;
    defaultChain: Chain;
}) => Promise<HostedSigner | null>;
export default getEthosSigner;
